#include "nala.h"
#include <pb-tools/partition.h>

TEST(basic)
{
/*
    const struct pb_partition_table_entry emmc0_boot_entries[] =
    {
        PB_PART_ENTRY(PB_PARTUUID_EMMC_BOOT, 0, 1024, 0, "Boot partition"),
        PB_PART_END,
    };

    const struct pb_partition_table emmc0_boot =
    {
        .name = "eMMC Boot",
        .block_size = 512,
        .entries = emmc0_boot_entries,
    };
*/
}
